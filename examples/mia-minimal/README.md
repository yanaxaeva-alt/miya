# Minimal Mia example

This directory is reserved for the first runnable MiaOS example.

Planned contents:

- minimal persona profile;
- `.mia` directory package;
- mock-provider chat transcript;
- simple AgentGraph specification.

The repository skeleton does not include business logic yet, so this example currently documents the target location for future vertical slices.
