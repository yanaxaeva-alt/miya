# Integration tests

Integration tests will be added with the runtime slices that exercise real command flows, model-provider interfaces, graph execution, and backend APIs.

The initial repository skeleton intentionally keeps this directory empty of executable tests until business logic exists.
